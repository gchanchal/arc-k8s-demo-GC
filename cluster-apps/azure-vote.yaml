apiVersion: apps/v1 # for versions before 1.9.0 use apps/v1beta2
kind: Deployment
metadata:
  name: reporting-dashboard-back-gc
spec:
  selector:
    matchLabels:
      app: reporting-dashboard-back-gc
  replicas: 1 # tells deployment to run 2 pods matching the template
  template:
    metadata:
      labels:
        app: reporting-dashboard-back-gc
    spec:
      containers:
      - name: reporting-dashboard-back-gc
        image: gchanchal/gauravs:back-v1.0
        imagePullPolicy: Always
        ports:
        - containerPort: 9000
---
apiVersion: v1
kind: Service
metadata:
  name: reporting-dashboard-back-gc
spec:
  type: NodePort
  ports:
  - port: 9000
    protocol: TCP
    targetPort: 9000
    nodePort: 30123
  selector:
    app: reporting-dashboard-back-gc
​
---
apiVersion: apps/v1 # for versions before 1.9.0 use apps/v1beta2
kind: Deployment
metadata:
  name: reporting-dashboard-front-gc
spec:
  selector:
    matchLabels:
      app: reporting-dashboard-front-gc
  replicas: 1 # tells deployment to run 2 pods matching the template
  template:
    metadata:
      labels:
        app: reporting-dashboard-front-gc
    spec:
      containers:
      - name: reporting-dashboard-front-gc
        image: gchanchal/gauravs:front-v1.0
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
​
---
​
apiVersion: v1
kind: Service
metadata:
  name: reporting-dashboard-front-gc
spec:
  type: NodePort
  ports:
  - port: 3000
    protocol: TCP
  selector:
    app: reporting-dashboard-front-gc
